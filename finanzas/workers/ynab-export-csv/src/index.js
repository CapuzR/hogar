// ynab-export-csv - PEGA AQUI el codigo del dashboard de Cloudflare.
// dash.cloudflare.com > Workers & Pages > ynab-export-csv > Edit code  (copia su src/index.js)
// Auth: header  X-Cap-Auth: <CAP_AUTH_TOKEN>.  Ver ../../../../docs/CONTEXT.md
export default {
  async fetch(request, env, ctx) {
    return new Response('TODO: pegar codigo de ynab-export-csv', { status: 501 });
  },
};

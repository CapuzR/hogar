name = "init-notion-fertilidad-schema"
main = "src/index.js"
compatibility_date = "2024-11-01"  # TODO: verifica que coincida con el dashboard (Settings)
# compatibility_flags = ["nodejs_compat"]   # descomenta si el worker lo requiere

# Secretos (NO van aqui). Cargalos con: wrangler secret put NOMBRE
#   CAP_AUTH_TOKEN (header X-Cap-Auth) + tokens YNAB/Notion segun el worker.

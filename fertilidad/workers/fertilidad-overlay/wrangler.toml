name = "fertilidad-overlay"
main = "src/index.js"
compatibility_date = "2026-04-19"
# compatibility_flags = ["nodejs_compat"]   # descomenta si el worker lo requiere

# Secretos (NO van aqui). Cargalos con: wrangler secret put NOMBRE
#   AUTH_TOKEN (header X-Cap-Auth del worker).

# KV que usa el worker (env.OVERLAY_KV). Sin esto, un deploy le quitaria el binding.
[[kv_namespaces]]
binding = "OVERLAY_KV"
id = "867bb940ba41434892ea694ffce09391"
